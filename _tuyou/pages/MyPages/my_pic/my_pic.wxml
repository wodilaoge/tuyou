<cu-custom bgColor="bg-blue" isBack="{{true}}">
  <view slot="backText">返回</view>
  <view slot="content">图片</view>
</cu-custom>
<view class="grid col-5 pad bg-white">
  <view class="margin-tb-sm " wx:for="{{Lists}}" wx:key bindtap="tabSelect" data-id="{{index}}">
    <button class="cu-btn  round bg-{{index==TabCur?'orange':'grey'}} width">{{item}}</button>
  </view>
</view>